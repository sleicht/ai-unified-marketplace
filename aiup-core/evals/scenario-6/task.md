# Library Management System — Use Case Diagram

## Problem Description

The City Public Library has finished a requirements-gathering sprint. The canonical catalogue at `docs/requirements.md` captures the functional requirements for library members and librarians, non-functional requirements, and constraints.

The team needs the Mermaid use case diagram embedded in that page so stakeholders can see who interacts with the system and what they can do. The stable diagram section exists but is empty.

## Output Specification

Update only the fenced `mermaid` block under `## Use Case Diagram` in `docs/requirements.md`. Preserve the requirements tables.
