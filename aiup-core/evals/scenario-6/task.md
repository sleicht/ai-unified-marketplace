# Library Management System — Use Case Diagram

## Problem Description

The City Public Library has finished a requirements-gathering sprint. The canonical catalogue at `docs/requirements.html` captures the functional requirements for library members and librarians, non-functional requirements, and constraints.

The team needs the Mermaid use case diagram embedded in that page so stakeholders can see who interacts with the system and what they can do. The stable diagram section exists but is empty.

## Output Specification

Update only `<pre class="mermaid">` inside `<section id="use-case-diagram">` in `docs/requirements.html`. Preserve the requirements tables.
